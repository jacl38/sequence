import { BoardSpace, Chip } from "../../store/types"

type CardProps = {
  cardType: BoardSpace,
  chip: Chip
}



export default function Card({ cardType, chip }: CardProps) {

  cardType
  
  return (
    <div>
      asdf
    </div>
  );
}